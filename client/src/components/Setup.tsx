import * as React from 'react'
import { useHistory } from 'react-router-dom'

export const Setup: React.FunctionComponent = (props) => {
  // https://discovery.meethue.com/
  return (
    <>
      <p>Looking for bridges ...</p>
    </>
  )
}
